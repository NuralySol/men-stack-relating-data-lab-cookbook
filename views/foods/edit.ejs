<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Food</title>
</head>
<body>
    <h1>Edit Food</h1>
    <form action="/users/<%= userId %>/foods/<%= food._id %>?_method=PUT" method="POST">
        <div>
            <label for="name">Name:</label>
            <input type="text" id="name" name="name" value="<%= food.name %>" required>
        </div>
        <div>
            <label for="calories">Calories:</label>
            <input type="number" id="calories" name="calories" value="<%= food.calories %>" required>
        </div>
        <div>
            <label for="date">Date:</label>
            <input type="date" id="date" name="date" value="<%= food.date ? food.date.toISOString().split('T')[0] : '' %>" required>
        </div>

        <button type="submit">Save Changes</button>
    </form>
</body>
</html>